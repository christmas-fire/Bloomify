import { useState } from 'react';
import './FlowerSidebar.css';
import FlowerFilterPanel from './FlowerFilterPanel';
import { apiFetch } from './api';

function FlowerSidebar({ onFilter, onFlowerAdded, onTokenRefresh }) {
    // Удаляем форму и фильтры, компонент больше не нужен
    return null;
}

export default FlowerSidebar; 